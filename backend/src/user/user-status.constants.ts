/**
 * @enum {string} Constantes que representan los estados de un usuario.
 * @readonly
 */
export enum UserStatusConstants {
	/**
	 * El estado representa cuando un usuario está activo.
	 */
	ACTIVE = 'ACTIVE',
	/**
	 * El estado representa cuando un usuario está jugando.
	 */
	PLAYING = 'PLAYING',
}